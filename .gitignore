# =============================================================================
# .gitignore — LGCP Deslizamientos Valle de Aburrá
# =============================================================================

# ─── DATOS GRANDES (superar límite de 100 MB de GitHub) ───────────────────────
# DEM original de alta resolución (3.4 GB) — almacenar en servidor externo o
# compartir mediante enlace (Google Drive, Zenodo, etc.)
DATA/MDT_05001_20241102-002.tif

# Rasters de covariables derivados del DEM (11–19 MB c/u)
# Son reproducibles ejecutando el notebook de Python
DATA/dem_10m.tif

# ─── ARCHIVOS GRANDES (>100MB) ────────────────────────────────────────────────
# Estos archivos superan el límite de GitHub y se mantienen localmente
DATA/dem_medellin_1m.tif
DATA/cov1m_pendiente.tif
DATA/dem_medellin_1m.tif.ovr
DATA/cov2m_pendiente.tif
DATA/dem_2m.tif
DATA/cov1m_aspecto.tif
DATA/cov2m_aspecto.tif
DATA/cov2m_twi.tif

# ─── ARCHIVOS TEMPORALES DE BASE DE DATOS ─────────────────────────────────────
DATA/*.gpkg-journal
DATA/*.gpkg-wal
DATA/*.gpkg-shm
*.sqlite-journal

# ─── CONFIGURACIÓN INTERNA DE COWORK / CLAUDE ─────────────────────────────────
.claude/

# ─── PYTHON ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.pyo
.Python
.env
.venv
env/
venv/
ENV/
.ipynb_checkpoints/
*.ipynb_checkpoints

# ─── R ────────────────────────────────────────────────────────────────────────
.Rhistory
.RData
.Rproj.user/
*.Rproj
*.rds
*.RDS
# Permitir archivos de resultados necesarios que no superan el límite de peso
!results_benchmark.rds
!results_ipp_2m.rds
!results_stratified.rds
# Archivos de caché de INLA (pueden ser muy grandes)
*.inla
INLA_results/

# ─── SISTEMA OPERATIVO ────────────────────────────────────────────────────────
# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
# Linux
*~

# ─── EDITORES ─────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# ─── OUTPUTS TEMPORALES / LOGS ────────────────────────────────────────────────
*.log
*.tmp
*.temp
*.bak
*.orig
nohup.out

# ─── LATEX: artefactos de compilación ────────────────────────────────────────
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.out
*.synctex.gz
*.synctex(busy)
# Descomentar la siguiente línea si no se quiere versionar el PDF compilado:
# *.pdf

# ─── SCRIPTS DE DIAGNÓSTICO Y CONFIGURACIÓN (intermedios, no son el modelo) ──
CODE/check_extent.R
CODE/check_extent.bat
CODE/check_nodes_covs.R
CODE/check_nodes_covs.bat
CODE/check_pkgs.R
CODE/check_pkgs.bat
CODE/check_rasters.R
CODE/check_rasters.bat
CODE/diag_confounding.R
CODE/diag_confounding.bat
CODE/diag_covs.R
CODE/diag_covs.bat
CODE/install_inla.R
CODE/install_inla.bat
CODE/mesh_stats.R
CODE/mesh_stats.bat
setup_github.bat
setup_github.sh
.gemini
.anthropic
.antigravity
.claud
